import * as M from '../mutation-consts';

const state = {
    
};

const getters = {

};

const actions = {

};

const mutations = {
    [M.CHANGE_PATH] (state) {
        state.paths = [];
    }
};

export default {
    state,
    getters,
    actions,
    mutations
}